;; End Widgets ;;
(defwidget end []
  (box :orientation "v"
       :space-evenly "false"
       :valign "end"
    ;; Contenedor para los botones de la barra con spacing uniforme
    (box :orientation "v" :space-evenly "false" :spacing -11
      (battery)
      (clock)
      (dashboard-button)
    )
  )
)